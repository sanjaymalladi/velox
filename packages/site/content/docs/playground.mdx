---
title: Playground
description: Interactive code playground to experiment with Velox animations.
---

# Playground

The playground is the fastest place to try the current Velox API without leaving the browser. It is best used for quick experiments, layout checks, and validating small scenes before you move back into a real `video.ts` file.

## What It Is Good For

- Testing builder syntax quickly
- Comparing animation presets
- Exploring aspect-ratio-safe layouts
- Verifying cards, charts, and simple scene ideas
- Exporting a quick browser WebM of the current custom code

## What It Is Not For

- Final MP4 render output
- Large multi-scene videos
- File-based project structure
- Asset-heavy production work

## Recommended Workflow

1. Start from one of the built-in examples.
2. Change one thing at a time.
3. Validate motion, spacing, and readability.
4. Use **Export WebM** when you need a quick browser-generated preview file.
5. Move the working code into `video.ts` or `reel.vml`.
6. Use `velox preview` and `velox render` for the production workflow.

## Example: Compact Builder Scene

```ts
import { createVideo, scene, text, shape } from '@velox-video/core'

export default createVideo({
  size: '720p',
  fps: 30,
  background: '#0a0a0f',
  scenes: [
    scene(4)
      .background(shape.gradient('135deg', '#0a0a0f', '#1a0a2e'))
      .add(
        text('VELOX')
          .center({ offsetY: -30 })
          .size(120)
          .weight(900)
          .color('#ffffff')
          .in('slideUp', 0.8),
        text('Motion Graphics Engine')
          .center({ offsetY: 60 })
          .size(32)
          .color('#a78bfa')
          .in('fadeIn', 0.6, { delay: 0.4 }),
      ),
  ],
})
```

## Example: Reel-Ready VML

```xml
<video size="portrait" fps="60" theme="creamChecks" background="creamGrid" motionQuality="premium">
  <scene duration="4.8" template="topTextBottomVisual" camera="slowPush" mood="editorial" transition="blurDissolve">
    <announcement slot="top" title="Launch reels from markup" subtitle="Templates, captions, assets, cards, and beats." badge="NEW" motion="heroCinematic" />
    <asset slot="visual" name="phone-frame" width="360" height="640" motion="driftIn" />
    <captions slot="caption" text="Launch reels from markup with safe VML." style="karaoke" />
  </scene>
  <scene duration="5.5" template="headlineThenProof" background="warmPaper" camera="slowPush" mood="editorial">
    <breakingNews slot="top" headline="No-key media starts free-first" ticker="Generated fallback, local, Wikipedia, Unsplash Source, OpenBrand metadata." tone="warning" />
    <githubRepo slot="visual" owner="sanjaymalladi" repo="velox" motion="driftIn" />
    <icon slot="overlay" name="github" size="104" motion="magneticPop" />
  </scene>
</video>
```

## Browser Export

The playground's **Export WebM** button records the current canvas animation with `MediaRecorder` and downloads a `.webm` file. It exports whatever is in the editor: VML, TypeScript builder code, or legacy examples that still compile.

This is meant for quick sharing and visual checks. For deterministic production export, use the CLI:

```bash
npx velox-video render reel.vml --output reel.mp4
```

## Tips

- Prefer explicit `size` or `aspectRatio`.
- Keep copy short while iterating.
- Check contrast before adding more effects.
- Use one visual idea per scene.
- If the result feels busy, remove elements before adding more animation.
- Prefer VML templates and slots for reels; they reduce manual positioning.
